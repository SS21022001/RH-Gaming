# Copy this to .env and customize
PORT=4000
# MongoDB connection string (local example)
MONGODB_URI=mongodb://127.0.0.1:27017/rh_gaming
# Example admin token (not secure) — if you later add admin-only routes you can check against this
ADMIN_TOKEN=change-me
